apiVersion: batch/v1
kind: Job
metadata:
  name: cbt-backup
  namespace: cbt-demo
  labels:
    app.kubernetes.io/name: cbt-backup
    app.kubernetes.io/component: backup
    app.kubernetes.io/part-of: k8s-cbt-s3mover-demo
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 600
  activeDeadlineSeconds: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cbt-backup
        app.kubernetes.io/component: backup
    spec:
      serviceAccountName: cbt-backup-sa
      restartPolicy: Never
      containers:
      - name: backup
        image: cbt-backup:latest
        imagePullPolicy: IfNotPresent
        command:
        - /usr/local/bin/cbt-backup
        - create
        - --pvc=block-writer-data
        - --namespace=cbt-demo
        - --device=/dev/xvda
        - --s3-endpoint=minio.cbt-demo.svc.cluster.local:9000
        - --s3-bucket=snapshots
        - --service-account=cbt-backup-sa
        env:
        - name: S3_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: minio-credentials
              key: root-user
        - name: S3_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: minio-credentials
              key: root-password
        volumeDevices:
        - name: data-volume
          devicePath: /dev/xvda
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        securityContext:
          privileged: true
      volumes:
      - name: data-volume
        persistentVolumeClaim:
          claimName: block-writer-data
