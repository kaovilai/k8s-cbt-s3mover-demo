apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: restored-data
  namespace: cbt-demo
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Block
  storageClassName: csi-hostpath-sc
  resources:
    requests:
      storage: 1Gi
